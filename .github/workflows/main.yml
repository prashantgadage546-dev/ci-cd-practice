name: Simple CI CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Run Test
        run: echo "Code checked successfully"

      - name: Deploy
        run: echo "Hello CI is now deployed 🚀"
